# MSH — 마석고등학교 수업 자료

마석고 2학년 영어 수업에서 **교실 크롬북으로 바로 여는** 웹 자료 모음.
GitHub Pages 가 `main` 브랜치 루트를 그대로 서빙한다 → **main 에 push 하면 즉시 라이브다.**

> https://totojunior.github.io/MSH/

학생이 링크 하나로 열고, 인터넷이 끊긴 교실에서는 HTML 파일 하나를 받아 더블클릭한다.
이 두 가지가 모든 기술 결정의 이유다.

---

## 1. 저장소 지도

| 경로 | 내용 | 구조 |
|---|---|---|
| `SR2/` | 외부지문 2 · **1차시** 트롤리 문제 (3D) | `src/` 모듈 → `assemble.js` → 단일 `index.html` |
| `SR2/BL/` | 외부지문 2 · **2차시** 출생 추첨 (롤스) | `src/` + python 빌드 → 단일 `index.html` |
| `SR2/NZ/` | 외부지문 2 · **3차시** 노직의 철학 법정 | `src/` 모듈 → `assemble.js` → 단일 `index.html` |
| `self-study.html` + `vocab-data/` | 어휘 자기주도학습 앱 (루트 `index.html` 이 여기로 리다이렉트) | 단일 HTML + 데이터 JS |
| `Lesson2/`, `review/`, `PNS/`, `hotdog/`, `stress/` | 개별 수업 자료 | 대부분 손으로 쓴 단일 HTML |

`SR2/README.md` 에 외부지문 2 차시별 링크 표가 있다. **새 차시를 추가하면 이 표에 행을 추가한다.**

---

## 2. 어디를 고쳐야 하나

- **수업 내용(사건·문항·문구)** → 각 자료의 데이터 파일. NZ 는 `SR2/NZ/src/cases.js` **한 파일**이다.
- **화면 문구** → NZ 는 `SR2/NZ/src/copy.js`. 코드에 한국어 리터럴을 박지 않는다.
- **`SR2/*/index.html` 은 빌드 산출물이다. 직접 고치지 마라.** `src/` 를 고치고 다시 굽는다.
- 교사가 github.com 웹 편집기에서 `src/` 를 고치면 **CI 가 알아서 다시 굽고 커밋한다**
  (`.github/workflows/sr2-build.yml`, `nz-build.yml`). 이 구조를 깨지 마라 — 교사가 노트북 없이
  교실에서 문장 하나를 고칠 수 있어야 한다는 것이 이 파이프라인의 존재 이유다.

---

## 3. 명령

```bash
# SR2/NZ (3차시)
node SR2/NZ/src/test/run.js      # 콘텐츠·상태·채점 불변식 80항목. CI 가 먼저 돌린다
node SR2/NZ/src/assemble.js      # src/ → SR2/NZ/index.html. 규칙 위반이면 종료 코드 1
node SR2/NZ/src/serve.js         # http://127.0.0.1:8767/ 로컬 확인

# SR2 (1차시)
node SR2/src/assemble.js
node SR2/src/serve.js            # http://127.0.0.1:8766/
```

**설치할 의존성이 없다.** `npm install` 하지 마라. `package.json` 도 없고 필요 없다.
node 와 브라우저만 있으면 된다. 새 빌드 도구·프레임워크·번들러를 들이지 마라.

`python -m http.server` 를 쓰지 마라 — charset 을 안 붙여서 한국어 로케일 브라우저가
EUC-KR 로 추측하고 본문을 전부 깨뜨린다. `serve.js` 가 그래서 있다.

---

## 4. 협상 대상이 아닌 제약 (assemble.js 가 기계로 막는다)

빌드된 페이지는 `<script type="text/plain">` 블록 안에 소스를 담고 부팅 때 주입한다. 따라서:

- **ES 모듈 금지**(`import`/`export`), `require()` 금지 → 각 모듈은 `(function(){ 'use strict'; ... })()` 로
  전역 하나(`window.NZ` 등)에 붙는다
- **`fetch()` / `XMLHttpRequest` 금지** — 오프라인 교실에서 죽는다
- **외부 URL 금지.** 예외는 구글 웹폰트 하나뿐이고, 못 받아도 대체 폰트로 굴러가야 한다
- 문자열 안에 리터럴 `</script` 금지 (파서가 거기서 블록을 닫는다)
- `TODO` / `FIXME` / `구현 예정` 같은 미완성 마커 금지
- **three.js 는 r134 에 핀 고정.** `SR2/NZ/src/THREE-CONSTRAINTS.md` 의 "NOT available" 목록을
  빌드가 검사한다. `CapsuleGeometry`, `OrbitControls`, `EffectComposer`, `BufferGeometryUtils`,
  `outputColorSpace`, `texture.colorSpace` 등은 **존재하지 않는다.** r134 가 cdnjs 의 마지막 UMD 빌드다.
- **학생 개인정보를 어떤 형태로도 수집·저장하지 마라.** 이름·학번·점수 전부. 서버도 DB도 로그인도 없다.

---

## 5. 수업 자료로서 지켜야 할 것

기술 제약보다 이쪽을 어기는 편이 더 나쁘다.

- **자료가 특정 입장을 정답으로 선언하지 않는다.** NZ 는 화면 어디에도 '정답/오답'을 쓰지 않고
  **'노직의 판단과 일치 / 다름'** 으로만 쓴다. 테스트가 이 문자열을 검사한다.
- **점수를 학생의 성향으로 읽히게 하지 마라.** 등급·순위·비교·공유 기능을 만들지 마라.
- **틀린 학생을 몰아붙이지 마라.** 맞힐 때까지 반복시키지 않고, 오답 대사는 학생의 인격이 아니라
  판단 근거를 겨눈다. 연속 오답일수록 반응이 조용해진다.
- **색상만으로 상태를 구분하지 마라.** 아이콘·글자·패턴을 함께 쓴다.
- **소리는 기본 꺼짐.** 자동 재생 금지. 교실에서 30대가 동시에 울린다.
- 자료가 5분을 넘겨도 **학생을 자동 종료시키지 마라.** 시간은 보여 주기만 한다.
- 각 자료 폴더에 **`README.md`(교사용 사용법)** 와 필요하면 `교사용-진행안.html`(인쇄용)을 둔다.
  `SR2/NZ/README.md` 와 `SR2/BL/README.md` 가 그 형식의 기준이다.

---

## 6. 하다가 실제로 깨졌던 것들

같은 데 두 번 빠지지 않도록 남긴다.

- **`<canvas>` 는 replaced element 다.** `position:fixed` + `left/right/top/bottom` 만으로는 **안 늘어난다.**
  고유 크기 300×150 으로 남는다. `width`/`height` 를 명시적으로 줘야 한다.
- **CSS 이스케이프를 손으로 쓰지 마라.** `content:"▤\00A0"` 를 쓰려다 리터럴 NUL 바이트가 들어가
  학생 화면에 깨진 글자(◆)가 떴다. 소스에 제어문자가 없는지 검사하라.
- **선언한 폰트와 불러오는 폰트를 일치시켜라.** 안 불러오는 폰트를 스택 맨 앞에 두면
  그게 깔린 PC 와 안 깔린 크롬북이 서로 다른 얼굴로 뜬다. 교실에서는 일관성이 우선이다.
- **`localStorage` 키에 자료별 접두사를 붙여라.** 모든 자료가 같은 origin 이다. NZ 는 `nz.progress` /
  `nz.prefs` 를 쓴다. 접두사가 없으면 앞 차시 기록을 덮어쓴다.
- **`setScene()` 직후 `pause()` 를 부르면 새 구도가 한 프레임도 안 그려진다.** 멈추기 전에 한 장 더 그려라.
- **3D 카메라는 글자가 앉을 자리를 비워 두고 잡아라.** 제목이 캐릭터 얼굴 위에 얹히는 사고가
  CSS 문제로 보이지만 대부분 카메라 문제다. 2D 폴백 구도도 같이 맞춰야 한다.
- **WebGL 프로브는 반드시 별도의 임시 캔버스로** 하고 `WEBGL_lose_context` 로 반납하라.
  실제 캔버스에서 프로브하면 컨텍스트를 점유해 렌더러 생성이 실패한다.
- 저장소 파일은 **CRLF** 다. 스크립트로 파일을 고칠 때 줄바꿈을 뭉개지 마라.

---

## 7. 작업 방식

- **주석과 커밋 메시지는 한국어로.** 무엇을 하는지가 아니라 **왜 그렇게 했는지**를 남긴다.
  기존 코드의 주석 톤을 따르라 — 이 저장소는 그게 문서 역할을 한다.
- 고친 뒤에는 **반드시 실제 브라우저에서 확인하라.** 테스트 통과는 필요조건일 뿐이다.
  캔버스 크기·글자 잘림·겹침은 헤드리스 크롬으로 스크린샷을 찍어 눈으로 봐야 잡힌다.
  주요 검증 크기는 **1366×768**(크롬북)이고, 1280×720 / 1024×768 / 좁은 폭도 확인한다.
- **테스트를 무르게 만들어 통과시키지 마라.** 검사를 지우거나 조건을 약화시키는 것은 실패로 친다.
- 큰 자료를 새로 만들 때는 `SR2/NZ/docs/` 처럼 **설계 근거와 검토 기록을 남긴다**
  (요구사항 원본, 화면 흐름·상태, 연출 명세, 레이아웃·접근성, 수업 운영, 교실 리스크 검토, 구현 계약).
